---
title: new
---

Create new target locale files for additional languages (copy base values).

```bash
i18nfix new --langs fr,ja
```

Options:
- `--langs <langs>` — comma-separated language codes
- `--skip-existing` — skip creating files that already exist
- `--no-update-config` — do not add created/existing targets into config
